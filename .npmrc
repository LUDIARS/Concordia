@ludiars:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# junction 禁止 (2026-07-02): file: 依存 (lib/vestigium) を junction ではなく実体コピーで入れる。
# junction だと npm install/ci が参照先 submodule の実体を消す (#261)。
install-links=true
