{
  "name": "observability",
  "description": "Cost, resource, and time-based reporting: aggregating spend, sampling host metrics, and scheduling daily/morning/stat rollups.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "(^|/)src/cost/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/metrics/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/daily/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/stat/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/morning/[^/]+$"
    }
  ],
  "cardTemplate": "Summarise the \"observability\" domain: Cost, resource, and time-based reporting: aggregating spend, sampling host metrics, and scheduling daily/morning/stat rollups."
}
