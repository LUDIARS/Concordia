{
  "name": "session-coordination",
  "description": "Core purpose: track concurrent AI coding agent sessions — register, share progress, detect lost sessions, recover from jsonl, resume, and generate end-of-session reports.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "(^|/)src/control/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/work/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/processes/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/report/[^/]+$"
    },
    {
      "pathPattern": "(^|/)src/providers/[^/]+$"
    }
  ],
  "cardTemplate": "Summarise the \"session-coordination\" domain: Core purpose: track concurrent AI coding agent sessions — register, share progress, detect lost sessions, recover from jsonl, resume, and generate end-of-session reports."
}
