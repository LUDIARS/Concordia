{
  "name": "parttimer-review-scheduling",
  "description": "パートタイマーの定期レビューを、Delegation テンプレートと Timer Delegation の cron 定義で登録・移行する責務。レビュー本文の評価や実行キューの払出しは含めない。",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "(^|/)src/delegation/seed(\\.test)?\\.ts$"
    },
    {
      "pathPattern": "(^|/)spec/tasks/2026-09-04-quaestor-mail-watch-renew\\.md$"
    },
    {
      "pathPattern": "(^|/)src/scheduler/cron-jobs(\\.test)?\\.ts$"
    },
    {
      "pathPattern": "(^|/)src/admin/(runtime-settings|state)\\.ts$"
    },
    {
      "pathPattern": "(^|/)src/bootstrap/core\\.ts$"
    },
    {
      "pathPattern": "(^|/)web/src/pages/Settings\\.tsx$"
    },
    {
      "pathPattern": "(^|/)web/src/pages/settings/sections/CronJobsSection\\.tsx$"
    },
    {
      "pathPattern": "(^|/)tests/(admin-api|admin-state|cron-scheduler|delegation-regression)\\.test\\.ts$"
    },
    {
      "pathPattern": "(^|/)spec/feature/delegation\\.md$"
    },
    {
      "pathPattern": "(^|/)spec/feature/team-standup-and-review\\.md$"
    },
    {
      "pathPattern": "(^|/)docs/manual/02-webui-guide\\.md$"
    },
    {
      "pathPattern": "(^|/)src/control/parttimer-clockout(\\.test)?\\.ts$"
    }
  ],
  "cardTemplate": "Summarise the \"parttimer-review-scheduling\" domain: scheduled parttimer review templates, cron registration, settings UI labels, and legacy schedule migration.",
  "mechanics": [
    "parttimer-template-seeding",
    "timer-delegation-cron-registration",
    "legacy-daily-review-deactivation",
    "legacy-cron-override-migration"
  ],
  "specRefs": [
    "spec/feature/delegation.md#9-初期-seed",
    "spec/feature/team-standup-and-review.md#1-チームごとの-fanout"
  ],
  "rationale": "定期レビューの有効状態はテンプレートだけでは完結せず、cron の call_name と一致して初めて Timer Delegation が起動する。設定 UI の説明も同じ cron 設定を利用者へ示す境界に属する。旧日次テンプレートの無効化と、管理者が保存した cron override を新 job 名へ引き継ぐ処理もこの登録の移行規則であり、レビュー内容や実行キューの責務とは分離する。"
}
