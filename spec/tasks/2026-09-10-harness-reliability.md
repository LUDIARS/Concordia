# ハーネス改善の実装

- Project: Concordia (Cc)
- Branch: feat/harness-reliability-20260910
- Workflow: Revisor local PR。作成後停止、merge/main更新なし。
- UX: UX-CC-W2/W3/W4/W5、UX-CC-S3/S4/S5
- Invariants: HR-01～HR-07、CC-INV-01/03

## 対応

1. ネイティブ圧縮の前後フック、保存・読み戻し・短い復旧参照。
2. 手動clearの保存失敗・配送未確認時停止と結果不明の再送防止。
3. Augurの空集計・宣言条件欠落の拒否、branchのみの確認との区別。
4. プロジェクトの設定表示とsessionの実観測パネル。
5. 初動・中間の確率抽出、人間入力の照合、公式指針に基づく助言と依頼者別アプローチ生成・SYSTEM投稿。
6. MCP認証エラーの分類・重複通知抑制と、claimを命令形だけで誤認しない非遮断の注入候補検知。
7. 時刻・乱数・LLM・通知を差し替える隔離fixtureと専用vitest設定。
8. 人間の丁寧な実装依頼を相談扱いへ戻さない規則の補足と問題ログ。

## 制限・未確認

導入・実配送・起動テスト・画面の実機表示は未確認。既存の起動中サービス・利用者設定は変更しない。公式設定点検はsnapshot入力APIで、全クライアント設定の自動収集ではない。モデル固有指針未確認の場合は共通指針に限定する。本社外SYSTEMへの配送、契約の起動時snapshotによる削除検知は未対応。

## 検証

backend・web・testsのTypeScript型検査が成功。ユーザーがテストを明示許可したため、サービスを起動しない隔離suiteをworktreeで実行した。起動テストは本体フォルダ・Excubitorに限定する。

## 2026-09-10 追加実装・検証

ユーザーからテストとオンタイムテストの実行許可を取得。初期の未実行記載は着手時点の履歴。最終確認はハーネス25件（実Augur往復1件を含む）と関連81件、計106件成功。旧APIの期待値3件を追加仕様へ更新後に再確認した。契約lintは2契約・指摘0。型チェック3系統成功。Cc testing claim/releaseを使用し、サービス起動・再起動なし。

DDD・テスト・オンタイムのproject設定、受入フックとlocal PR検査、固定スクリプトの契約挿入・集計、Pf未登録fallback、実ツール利用・反復失敗・外部更新結果不明の観測を追加。AIFormatに横断設計、Castraに参照登録を追加。本体反映・hook信頼・Discord配送・本番効果は未確認。
