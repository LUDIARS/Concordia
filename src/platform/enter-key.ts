/**
 * Text injected to mean a physical Enter key press in wrapped terminal CLIs.
 * CR is what terminals normally send for Enter; LF can be treated as a
 * multiline insert by some GPT/Codex-style composers.
 */
export const ENTER_KEY_TEXT = "\r";
